#the format is in a x by x matrix in a bunch of numbers and commas so need to create a function to process a bunch of numbers into a matrix
def ttm(text,size):#text to matrix
    list of numbers = []
    for i in range(0,len(text)):
